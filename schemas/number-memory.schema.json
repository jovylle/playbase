{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://github.com/jovylle/playbase/schemas/number-memory.schema.json",
  "title": "Number Memory Score",
  "description": "Schema for number memory game scores",
  "type": "object",
  "properties": {
    "digits": {
      "type": "integer",
      "minimum": 1,
      "maximum": 20,
      "description": "Longest correctly recalled digit sequence length. Unlike reaction's ms (lower is better), higher digits is better here — backend sorting and isNewRecord logic must treat this metric as higher-is-better."
    },
    "timestamp": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 timestamp when the score was achieved"
    },
    "id": {
      "type": "string",
      "pattern": "^[a-f0-9]{8}$",
      "description": "8-character hex ID for the score"
    },
    "playerName": {
      "type": "string",
      "description": "Display name of the player who submitted the score"
    },
    "playerId": {
      "type": "string",
      "description": "Anonymous fingerprint identifying the player"
    }
  },
  "required": ["digits", "timestamp", "id", "playerName", "playerId"],
  "additionalProperties": false
}
