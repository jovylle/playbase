name: History Maintenance Placeholder

on:
  workflow_dispatch:

jobs:
  season-reset-disabled:
    runs-on: ubuntu-latest

    steps:
      - name: Quarterly reset is disabled
        run: |
          echo "Quarterly season resets are disabled."
          echo "The reaction leaderboard is now all-time."
          echo "Full score history is available in reaction/history.json and /history.html."
